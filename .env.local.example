# Google Cloud Console Configuration
# Follow the setup instructions in docs/GOOGLE_SETUP.md to get these values

# Google OAuth 2.0 Client ID (public)
# Get from: Google Cloud Console > APIs & Services > Credentials
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your_client_id_here.apps.googleusercontent.com

# Google OAuth 2.0 Client Secret (server-side only, keep private)
# Get from: Google Cloud Console > APIs & Services > Credentials
GOOGLE_CLIENT_SECRET=your_client_secret_here

# OAuth 2.0 Redirect URI (public)
# Must match exactly with what's configured in Google Cloud Console
# For development: http://localhost:3000/auth/google/callback
# For production: https://yourdomain.com/auth/google/callback
NEXT_PUBLIC_GOOGLE_REDIRECT_URI=http://localhost:3000/auth/google/callback

# Optional: Google Analytics for medical app usage (anonymized)
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Security Configuration
# Change this in production to a secure random string
NEXTAUTH_SECRET=your_nextauth_secret_here_change_in_production

# Application URLs
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXTAUTH_URL=http://localhost:3000

# Medical Data Privacy Settings
# Set to 'true' to enable additional privacy features
NEXT_PUBLIC_HIPAA_MODE=false
NEXT_PUBLIC_GDPR_MODE=false

# Development/Debug Settings
# Set to 'true' in development only
NEXT_PUBLIC_DEBUG_MODE=false
NEXT_PUBLIC_ENCRYPTION_DEBUG=false